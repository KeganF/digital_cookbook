{{!==============================================================================}}
{{! FILE : recipe_card.handlebars (./views/partials/recipe_card.handlebars)      }}
{{! DESC : This file contains the markup for the recipe card partial that can be }}
{{!        reused throughout the app.                                            }}
{{!==============================================================================}}

{{!===========================recipe_card.handlebars=============================}}
<a class="card-link" href="/recipe?Id={{recipeID}}">
    <div class="card flex-shrink-0" style="width:18rem; height:26.5rem;">
        <img class="card-img-top" src="{{image}}" alt="">
        <div class="card-body d-flex flex-column justify-content-between">
            <h5 class="card-title">{{label}}</h5>

            <div class="d-flex flex-wrap" style="gap:0.25rem;">
                {{#each tags}}
                    <span class="badge badge-primary">{{this}}</span>
                {{/each}}
            </div>
        </div>
    </div>
</a>
{{!===========================recipe_card.handlebars=============================}}